import { InterviewPrepPage } from './components/InterviewPrepPage.js';

const root = window.ReactDOM.createRoot(document.getElementById('root'));

root.render(
  window.React.createElement(window.React.StrictMode, null, window.React.createElement(InterviewPrepPage)),
);

